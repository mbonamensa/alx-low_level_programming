#include "main.h"

/**
 * _puts_recursion - prints a new line
 *
 * Return: 0 
 */

void _puts_recursion(char *s);

int main() {
_puts_recursion(*s);
return (0);
}

_puts_recursion() {
	_putchar("This is my string %s\n", *s);
	return (*s);
}
